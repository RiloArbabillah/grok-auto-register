services:
  grok-register:
    build:
      context: .
    image: grok-register:local
    container_name: grok-register
    init: true
    shm_size: "2gb"
    volumes:
      - .:/app
    working_dir: /app
    command: ["sleep", "infinity"]
